import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	btnContainer: {
		flex: 1,
		alignItems: 'center',
		borderRadius: 180,
		padding: 7,
		margin: 9,
	},
	btnIcon: {
		height: 30,
		width: 30,
	},
});

export default styles;
